export default function ClassTopicContent({ open }: { open: boolean }) {
    return (
        <div>
            ClassTopicContent
            {/* {classData?.subTopics?.map((_, j: number) => (
                <SubTopic key={`sub-topic-${_.id}`} data={_} />
            ))} */}
        </div>
    );
}
